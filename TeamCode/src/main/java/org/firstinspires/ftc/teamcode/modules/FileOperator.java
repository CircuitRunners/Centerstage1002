package org.firstinspires.ftc.teamcode.modules;

public class FileOperator {
    // this will need to have a constructor to specify which file folder, and have a few constants also defined
    // for example thisFile = new FileOperator(FileOperator.FIRST_Folder)
}
